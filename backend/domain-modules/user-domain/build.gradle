dependencies {
    implementation project(':domain-modules:image-domain')
    testImplementation(testFixtures(project(':domain-modules:image-domain')))
    testImplementation(testFixtures(project(':domain-modules:user-domain')))
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}