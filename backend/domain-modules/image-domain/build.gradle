dependencies {
    implementation project(':infrastructure-modules:s3-client-module')

    testImplementation(testFixtures(project(':domain-modules:image-domain')))

}

bootJar {
    enabled = false
}

jar {
    enabled = true
}